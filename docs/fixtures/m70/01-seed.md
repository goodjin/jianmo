# Seed Title

一段用于生成长文档的种子文本。包含常见结构：

- 列表项 A
- 列表项 B（含 [内部链接](./other.md#section)）

> 引用块：用于模拟笔记摘录。

```ts
export function seed(x: number) {
  return x + 1;
}
```

## Section

这里是一段普通段落。这里是一段普通段落。这里是一段普通段落。

